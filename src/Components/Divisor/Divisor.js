import React from 'react';

import "./divisor.css";

function Divisor() {
    return (
        <div className="divisorParent">
            <div className="divisor"></div>
        </div>

    );
}

export default Divisor;